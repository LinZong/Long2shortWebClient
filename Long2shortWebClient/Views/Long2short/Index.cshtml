@model List<Long2shortWebClient.Models.Long2shortViewModel>

@{
    ViewData["Title"] = "Long2short Archive";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Long2short Archive</h2>

<div>
    @foreach (var m in Model)
    {
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(i => m.uid)
            </dt>
            <dd>
                @Html.DisplayFor(i => m.uid)
            </dd>
            <dt>
                @Html.DisplayNameFor(i => m.shortid)
            </dt>
            <dd>
                @Html.DisplayFor(i => m.shortid)
            </dd>
            <dt>
                @Html.DisplayNameFor(i => m.longid)
            </dt>
            <dd>
                @Html.DisplayFor(i => m.longid)
            </dd>
        </dl>

    }
</div>
<div>
    @Html.ActionLink("Create One", "GetShortUrl", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
